services:
  kokoro-tts:
    build:
      context: ./docker/kokoro
      dockerfile: Dockerfile
    container_name: vani-kokoro-tts
    ports:
      - "8100:8100"
    environment:
      - KOKORO_VOICE=af_heart
      - KOKORO_SPEED=0.95
    restart: unless-stopped
