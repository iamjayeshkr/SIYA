# Vani OS — P0 dependencies
# pip install -r requirements-p0.txt

# Structured logging
structlog>=24.0.0

# Async SQLite
aiosqlite>=0.20.0

# OS keychain (macOS Keychain, Windows Credential Store, etc.)
keyring>=25.0.0

# .env file loading (existing dep — keep for migrate_secrets fallback)
python-dotenv>=1.0.0
